# BQA pull-recovery prompts

`base.md` is combined with the matching `pull/*.md` template at runtime.
Templates contain policy and repair scope only. Runtime evidence is inserted after redaction; credential values never enter a prompt or session record.

`CTF-PULL-D99` is the fallback for an unclassified crash.
