# [CTF-PULL-D03]

Incident class: authentication failed or a private contest returned no challenges.

Distinguish expired credentials from a genuinely empty event and API drift. Ask for a fresh user cookie when authentication is expired.
